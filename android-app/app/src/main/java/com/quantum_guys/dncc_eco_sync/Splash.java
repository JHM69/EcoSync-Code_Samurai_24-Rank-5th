package com.quantum_guys.dncc_eco_sync;import android.content.Context;import android.content.Intent;import android.content.SharedPreferences;import android.os.Bundle;import androidx.appcompat.app.AppCompatActivity;import com.quantum_guys.dncc_eco_sync.activity.LoginActivity;public class Splash extends AppCompatActivity {    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        // Check if user is logged in        SharedPreferences sharedPreferences = getSharedPreferences("myPrefs", Context.MODE_PRIVATE);        boolean isLoggedIn = sharedPreferences.getBoolean("logged", false);        if(isLoggedIn) {            // If user is logged in, navigate to MainActivity            startActivity(new Intent(Splash.this, MainActivity.class).setFlags(Intent.FLAG_ACTIVITY_CLEAR_TASK | Intent.FLAG_ACTIVITY_NEW_TASK));            finish();        } else {            // If user is not logged in, navigate to LoginActivity            startActivity(new Intent(Splash.this, LoginActivity.class));            finish();        }    }}