package com.quantum_guys.dncc_eco_sync;import static com.quantum_guys.dncc_eco_sync.global.UserType.TYPE_ADMIN;import static com.quantum_guys.dncc_eco_sync.global.UserType.TYPE_DRIVER;import static com.quantum_guys.dncc_eco_sync.global.UserType.TYPE_LANDFILL_MANAGER;import static com.quantum_guys.dncc_eco_sync.global.UserType.TYPE_STS_MANAGER;import android.app.ActivityOptions;import android.content.Context;import android.content.Intent;import android.content.SharedPreferences;import android.content.res.Configuration;import android.os.Bundle;import androidx.appcompat.app.AppCompatActivity;import com.quantum_guys.dncc_eco_sync.activity.LoginActivity;import com.quantum_guys.dncc_eco_sync.global.FirstActivity;import com.quantum_guys.dncc_eco_sync.global.LanguageSetting;import com.quantum_guys.dncc_eco_sync.global.UserType;import java.util.Locale;public class Splash extends AppCompatActivity {    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        Locale locale = new Locale(LanguageSetting.getLanguage(this));        Locale.setDefault(locale);        Configuration config = getBaseContext().getResources().getConfiguration();        config.setLocale(locale);        getBaseContext().getResources().updateConfiguration(config,                getBaseContext().getResources().getDisplayMetrics());        int type = UserType.loadData(getApplicationContext());        SharedPreferences sharedPreferences = getSharedPreferences("myPrefs", Context.MODE_PRIVATE);        boolean isLoggedIn = sharedPreferences.getBoolean("logged", false);        if (type == TYPE_ADMIN) {            if (isLoggedIn) {                startActivity(new Intent(this, MainActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            } else {                startActivity(new Intent(this, LoginActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            }            finish();        } else if (type == TYPE_STS_MANAGER) {            if (isLoggedIn) {                startActivity(new Intent(this, MainActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            } else {                startActivity(new Intent(this, LoginActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            }            finish();        }else if (type == TYPE_LANDFILL_MANAGER) {            if (isLoggedIn) {                startActivity(new Intent(this, MainActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            } else {                startActivity(new Intent(this, LoginActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            }            finish();        } else if (type == TYPE_DRIVER) {            startActivity(new Intent(this, MainActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            finish();        } else {            startActivity(new Intent(this, FirstActivity.class), ActivityOptions.makeSceneTransitionAnimation(this).toBundle());            finish();        }          }}